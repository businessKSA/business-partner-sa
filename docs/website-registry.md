# سجلّ الموقع — من يملك كل صفحة

فحص `site/` يوم 2026-09-23. كل رقم هنا مقروء من المستودع لا مُقدَّر.

## الأرقام الحقيقية

| | العدد |
|---|---|
| مجموع ملفات HTML | 1218 |
| منها نسخ لغات (ar · zh · fr · es · ru · ko · ja · hi) | 1154 |
| **صفحات جذرية فعلية** | **64** |
| صفحات الخدمات `services/` | 149 |
| صفحات الوظائف `jobs/` | 38 |

**الخلاصة المهمة:** الموقع ليس 1218 صفحة. هو **64 صفحة** مترجمة إلى ثماني لغات.
الرقم الضخم أوهم أكثر من مرة بأن الفوضى أكبر مما هي.

## التصميم: 57 موحّدة، 7 خارجها

القياس: هل تستدعي الصفحة `assets/css/styles.css` (تصميم الموقع الرئيسي)؟

| الحالة | العدد |
|---|---|
| ✅ على التصميم الجديد | **57** |
| ❌ خارج التصميم | **7** |

السبعة، وكلها لوحات لا صفحات عامة:

| الصفحة | ما هي | الأسطر |
|---|---|---|
| `account.html` | مركز عمليات العميل | 4233 |
| `admin.html` | لوحة تحكم الموقع | 4911 |
| `monitor.html` | BP Inbox — محادثات العملاء | 863 |
| `portal.html` | بوابة الموظفين الأذكياء | 479 |
| `connect.html` | موظفك الذكي — رحلة العميل | 417 |
| `doc-agent-admin.html` | مستشار المستندات | — |
| `dashboard.html` | لوحة فريق الوكلاء | 212 |

**هذا ليس عطلاً بالضرورة.** لوحة تشغيل لا تحتاج تصميم صفحة تسويقية. لكنها
تحتاج **هوية موحّدة** — شعار وخط وألوان — وهذا ما ينقص.

## التكرار الحقيقي

هنا المشكلة، لا في التصميم.

### ثلاث لوحات للوكلاء — نفس الغرض

| اللوحة | أين | الحالة |
|---|---|---|
| `dashboard.html` | الموقع | 212 سطراً |
| Agent Control Center | داخل n8n (`6jgqpWlBRbyncwTC`) | تعمل، بلا بوابة دخول |
| `ai-space/` | Azure (لم تُنشر) | الكود جاهز |

**يجب أن تبقى واحدة.** المرشّحة: `ai-space` على Azure — لأنها الوحيدة التي
ستحمل بوابة دخول حقيقية بالدور `owner`.

### ثلاث واجهات للعميل

`account.html` (4233 سطراً — الحقيقية، ومربوطة من تذييل كل صفحة) ·
`my.html` (439) · `portal.html` (479).

### أربع واجهات إدارية

`admin.html` · `ops.html` · `monitor.html` · `doc-agent-admin.html`

## الاكتشاف الأهم: كلها مخفية

**لا واحدة من التسع لوحات مذكورة في قائمة التنقّل في الصفحة الرئيسية.**
فحص `ar/index.html`: صفر روابط إلى `account` أو `admin` أو `portal` أو
`dashboard` أو `ops` أو `monitor` أو `my` أو `connect`.

`/account` وحدها مربوطة — من **تذييل** الصفحات لا من القائمة.

فالعميل لا يعرف أن له حساباً إلا إن نزل إلى أسفل الصفحة. وهذه فجوة في رحلة
المستخدم أكبر من أي اختلاف ألوان.

## الملكية المقترحة

| المجموعة | الصفحات | المالك المقترح |
|---|---|---|
| الموقع العام | 57 صفحة + `services/` + `jobs/` | Website Agent |
| بوابة العميل | `account` (وتُدمج فيها `my` و`portal`) | Client Portal Agent |
| اللوحات الإدارية | `admin` · `ops` · `monitor` · `doc-agent-admin` | Ops Agent |
| لوحة الوكلاء | `ai-space` وحدها بعد الدمج | AI Workforce Agent |

**قاعدة: صفحة واحدة → مالك واحد.** لا تُسنَد صفحة لمالكين.

## أهم خمسة إصلاحات

| # | الإصلاح | الأولوية |
|---|---|---|
| 1 | بوابة دخول على لوحة الوكلاء (حمايتها الآن سرّية الرابط) | **P0** |
| 2 | اختيار لوحة وكلاء واحدة من الثلاث وأرشفة الباقي | **P0** |
| 3 | رابط ظاهر لحساب العميل في القائمة لا في التذييل وحده | **P1** |
| 4 | دمج `my` و`portal` في `account` مع تحويل 301 | **P1** |
| 5 | توحيد الهوية (شعار · خط · ألوان) على اللوحات السبع | **P2** |

## ما لم أفحصه

الموقع الحيّ `www.businesspartner.sa/ar` — فحصت المستودع لا المتصفّح.
إن كان المنشور يختلف عمّا في `site/` فالفرق يظهر بمقارنة النشرة الحيّة،
وهذه خطوة منفصلة.
