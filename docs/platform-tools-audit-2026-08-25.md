# 🧭 التدقيق الشامل للمنصة — 25 أغسطس 2026

> تدقيق CTO عميق مبني على فحص فعلي (لا افتراضات): مستودع GitHub بفرعيه، Vercel (7 مشاريع)،
> Supabase (مشروعان)، n8n (98 وركفلو)، Make، Notion، Gmail، ولوحة /admin على فرع الإنتاج.
> كل رقم في هذا التقرير مأخوذ من استعلام مباشر بتاريخ 2026-08-25.

---

## 🔴 أولاً: خمس نتائج حرجة (P0) — قبل أي شيء آخر

| # | الاكتشاف | الدليل | الإجراء المطلوب |
|---|---------|--------|-----------------|
| 1 | **قاعدة bp-quotes بلا حماية RLS إطلاقاً** — كل جداولها الـ23 (عملاء، هويات، فواتير، محفظة، روابط دخول MagicLink) مكشوفة لأي شخص يملك مفتاح anon | فحص Supabase الرسمي: تحذير critical على 23 جدولاً | تفعيل RLS مع سياسات، أو حصر الوصول بالـ service key فقط وإلغاء anon |
| 2 | **مفاتيح API حية مكشوفة داخل نوشن** — صفحة «🔴 عاجل — مفاتيح API مكشوفة» + صفحة «🔑 BP Inbox — الرابط والمفاتيح» | موجودتان في المساحة حتى الآن رغم تحذير تدقيق 2026-08-20 | تدوير كل المفاتيح فوراً ثم حذف محتوى الصفحتين |
| 3 | **الإنتاج لا يُنشر من master** — فرع الإنتاج الفعلي على Vercel هو `claude/bpic-marketing-site-jvrnga` وهو متقدم على master بـ**344 كوميت** (وmaster متقدم عليه بـ23) | سجل نشر Vercel: كل نشرات production من هذا الفرع | دمج الفرع في master وإعادة ضبط production=master، وإلا فأي عمل على master لن يصل الموقع أبداً |
| 4 | **المستودع عام (public)** — كل كود اللوحة والـ API والمنطق الداخلي مرئي للجميع | `githubRepoVisibility: public` في بيانات النشر | تحويله إلى private (لا توجد أسرار بالكود نفسه، لكن كشف منطق اللوحة والمسارات غير مقبول) |
| 5 | **Vercel على خطة Hobby** — الاستخدام التجاري مخالف لشروط Hobby، ولا يوجد SLA | `plan: hobby` من Vercel API | الترقية إلى Pro (~$20/شهر) |

---

## 1) جرد الأدوات والكونيكتورز — 18 متصلاً + 3 معطّلة

### المتصلة والعاملة (مثبت بالفحص)

| الأداة | دورها الفعلي عندك | التكلفة الشهرية | حدودك الحالية | كيف تطوّرها |
|--------|--------------------|------------------|----------------|--------------|
| **Notion** | العقل التشغيلي: CRM، كتالوج الخدمات (98)، ATS، العقود (37)، المراسلات الحكومية (235)، Names (6,838) | تقدير $10–30/مقعد حسب الباقة + AI — **تحقق من فاتورتك** | صفحات مفاتيح مكشوفة؛ قواعد فارغة (Client Projects, Vendors, Approvals) | تدوير المفاتيح، تعبئة أو أرشفة الفارغ، الالتزام بدليل القواعد |
| **n8n** | 98 وركفلو (47 نشط): واتساب الرئيسي، مُعين، Email→CRM، الكنس اليومي 06:00، ATS، فريق الإيجنتات الـ12 | Cloud Starter ~€24 أو Pro ~€60 — **تحقق** | لا يوجد مخطط موحد يجمعها؛ 51 وركفلو معطل بعضها مكرر | لوحة تصنيف بالوسوم (بدأت)؛ حذف/أرشفة المعطل المكرر |
| **Supabase** | مشروعان: businessKSA (بوابة العميل، 38 جدولاً + تحليلات الموقع) و bp-quotes (عروض/عقود/فواتير، 23 جدولاً) | **Free — مؤكد** | حد 500MB، إيقاف عند الخمول، **لا نسخ احتياطي زمني**، RLS معطل في bp-quotes | ترقية Pro ($25) عند أول عميل مدفوع فعلي + إصلاح RLS |
| **Vercel** | الاستضافة: 7 مشاريع، أهمها الموقع (1,098 صفحة) و bp-quotes | **Hobby (مجاني) — مؤكد** | استخدام تجاري على خطة غير تجارية؛ Web Analytics غير مفعّل (0 زيارة مسجلة) | Pro $20/شهر |
| **GitHub** | مستودعان؛ الرئيسي فيه 265+ PR | مجاني | المستودع public؛ التشتت بين 30+ فرع | private + توحيد الفروع |
| **Gmail** | بريد الشركة: 3,681 وارد (141 غير مقروء)، 3,613 صادر، 60+ تصنيف منظم | ضمن Google Workspace ~SAR 28/مستخدم — **تحقق** | 141 رسالة غير مقروءة | وركفلو Email→Notion CRM نشط ✓ — أضِف خلاصته للتقرير اليومي |
| **Google Drive / Calendar** | تخزين السير الذاتية ومستندات العملاء / حجوزات المقابلات | ضمن Workspace | — | — |
| **Make** | حساب قائم (توثيق ثنائي مفعّل) لكن آخر دخول 3 يوليو — **غير مستخدم فعلياً** | غالباً Free | ازدواجية مع n8n بلا فائدة | قرار: إلغاؤه أو تخصيصه لغرض محدد |
| **Anthropic API (Claude)** | وكيل الموقع (api/chat)، وكلاء n8n، قراءة المستندات | حسب الاستهلاك | لا مراقبة إنفاق مركزية | ضع Spend Alert في Console |
| **OpenAI API** | إيجنتات الفريق التنفيذي في n8n + تحليل CVs | حسب الاستهلاك | نفس الشيء | نفس الشيء |
| **Gemini + Groq** | قراءة الشهادات الضريبية / بديل سريع | مجاني | حدود معدل الاستخدام | — |
| **Moyasar** | بوابة الدفع (بطاقات + Apple Pay) | نسبة لكل عملية (~1% مدى / ~2.75% بطاقات — راجع عقدك) | — | — |
| **Resend** | إرسال البريد من الـ API | Free حتى 3k/شهر ثم $20 | — | — |
| **DocuSign** | توقيع العقود | **بيئة demo مجانية — التوقيعات غير ملزمة قانوناً!** | يحتاج DOCUSIGN_ENV=production + اشتراك (~$25–40/شهر) قبل أي عقد حقيقي | — |
| **الدفترة (Daftra)** | الفواتير الضريبية | اشتراك سنوي — **تحقق** | الجسر مغلق افتراضياً (#239) | — |
| **WhatsApp Cloud API** | رقم الشركة +966507034157 (الأوركستريتور) + مُعين | حسب المحادثات (تسويقية/خدمية) | قالب bp_intro_ar لم يُعتمد بعد → حملة الواتساب اليومية معطلة | اعتماد القالب |
| **Gamma / Apify / Expedia / Kiwi / Indeed / Slack** | متصلة عبر كلود، استخدام خفيف أو معدوم | Gamma ~$10–20 إن كنت مشتركاً؛ الباقي مجاني/حسب الاستخدام | Slack غير مستخدم فعلياً | قرار إبقاء أو فصل |
| **Apollo.io** | إثراء بيانات الشركات | credits حسب الباقة | **انقطع التوثيق — يحتاج إعادة ربط** | أعد الربط من إعدادات الكونيكتورز |

### معطّلة في الشات (متصلة على مستوى الحساب)
Atlassian Rovo، Canva، Microsoft 365 — قرارك: تفعيل أو إزالة.

> ⚠️ أرقام التكلفة المكتوبة «تحقق» تقديرات من الأسعار المعلنة — ليس عندي وصول لفواتيرك.
> **النطاق التقديري الكلي للاشتراكات الحالية: ~SAR 400–900/شهر** (يرتفع ~SAR 350 بعد ترقيات P0: Vercel Pro + Supabase Pro + DocuSign production).

---

## 2) الموقع الإلكتروني — businesspartner.sa

**الحجم الفعلي (فرع الإنتاج):** 1,098 صفحة HTML — 56 قالباً رئيسياً × 9 لغات (AR/EN كاملتان، FR كاملة، ES/ZH/RU/HI/KO/JA أساسية) + 95 صفحة خدمة + 9 تصنيفات + 26 صفحة وظيفة + 6 حاسبات + 12 صفحة فريق.

**قنوات الموقع الرئيسية ورحلة العميل في كل منها:**

| القسم | الصفحات | الرحلة | الحالة والنواقص |
|-------|---------|--------|------------------|
| التسويقي | `/` `/about` `/services/*` `/packages` `/contact` `/consultation` | زيارة ← خدمة ← سلة ← checkout أو واتساب | ✅ مكتمل. ناقص: صفحة اتصل بنا فيها رقم قديم (مسجل في نوشن منذ فترة ولم يُصحح) |
| التجارة | `/cart` `/checkout` `/account` | سلة ← دفع مُيسّر ← حساب العميل ← تتبع الطلب | ✅ يعمل: 8 طلبات بقيمة 5,001 ﷼ في القاعدة. ناقص: الطلبات كلها اختبارية تقريباً |
| التوظيف | `/careers` `/jobs/*` `/hr` `/employers` + بوابة hr.businesspartner.sa | مرشح: تقديم ← فرز AI ← ATS نوشن / صاحب عمل: دخول برمز | ✅ نشط (وركفلو ATS يعمل). ناقص: توحيد بوابتي employer-login و portal |
| الشركاء والموردون | `/suppliers` `/partner-dashboard` + ضمانات escrow | تسجيل ← توثيق بريد ← لوحة شريك ← محفظة/ضمان | ✅ أُصلح التسجيل المعطوب في #266. جداول escrow فارغة (لم يُستخدم بعد) |
| الوكلاء الأذكياء | `/ai-agents` `/compliance-agent` `/portal` `/shared-services` | تجربة شات ← اشتراك (250/500/1500 ﷼ شهرياً) | ✅ الوكلاء يردون عبر n8n. أُضيفت للكتالوج حديثاً |
| عروض الأسعار والعقود | تطبيق quotes (Next.js) على bp-quotes | عميل ← عرض سعر برابط ← توقيع ← فاتورة ← سداد برمز بلا دخول | ✅ مبني كاملاً. 🔴 قاعدة بياناته بلا RLS؛ DocuSign تجريبي |
| مركز المعرفة | `/saudi-arabia` `/magazine` `/news` `/newsletter` `/tools-and-calculators` | محتوى ← نشرة (كرون أحد 06:00) | ⚠️ مشتركو النشرة = 2 فقط |
| قنوات أخرى | `/deals` `/estrdad` `/bank-account` `/formation-contract` `/mahfol-makfol` `/tourism` `/worker-housing` `/workspaces` `/farina` `/data` | نماذج طلب ← CRM | ✅ موجودة. عمقها التشغيلي متفاوت |

---

## 3) البوابات واللوحات — 14 بوابة

**للمالك (أنت):**
1. **`/admin` — لوحة التحكم الرئيسية** (4,033 سطراً على فرع الإنتاج): نظرة عامة + إحصائيات زوار أولية الطرف + متابعات اليوم (CRM تفاعلي يكتب في نوشن مباشرة) + الطلبات والموافقات + الشركاء وأوامر العمل + محرر محتوى الموقع (يحفظ في GitHub وينشر تلقائياً) + الفواتير وجسر الدفترة + **قائمة «الخدمات المتصلة»: فحص حي لـ18 خدمة** (المفاتيح، الدفع، نفاذ، اعتماد، واتساب، نوشن، قاعدة البيانات…)
2. **`/monitor` — BP Inbox**: كل محادثات واتساب والموقع، إيقاف الوكيل والرد بنفسك، إرسال وسائط، اقتراح رد بالذكاء، وأزرار اتصال/واتساب/إيميل لكل محادثة
3. لوحات فرعية: `/suppliers-admin` `/agencies-admin` `/jobsearch-admin` + `quotes/admin` (عروض/عقود/فواتير/كتالوج/عملاء/سجل تدقيق)

**للعميل:** `/account` (الغلاف المركزي) · `quotes/portal` · بوابة الامتثال · بوابة الخدمات المشتركة (رموز BP-SS) · لوحة صاحب العمل · لوحة الشريك.

**نموذج أولي فقط:** `/revenue-os` و `/client-portal` — **بيانات تجريبية، بلا دخول حقيقي ولا عزل** (موثق في نوشن). لا يُرسل لعميل.

### إجابة سؤالك: «هل كل شيء ملموم في مكان واحد؟»
**البنية موجودة والتجميع يعمل، لكن ليس شاشة واحدة بعد:**
- ✅ طلبات الموقع + واتساب الشركة + التذاكر + الاستشارات → كلها في **Sales Pipeline** واحد في نوشن وتظهر في `/admin` (متابعات اليوم)
- ✅ الإيميل → وركفلو Email→Notion CRM نشط يصنف ويؤرشف
- ✅ **الكنس اليومي 06:00**: خلاصة يومية موحدة تصلك واتساب + إيميل، وتذكير مسائي 16:00
- ⚠️ الناقص: بريد Gmail لا يظهر داخل `/admin` نفسها؛ بيانات bp-quotes (عروض/فواتير) منفصلة عن الكنس اليومي؛ Slack والتسويق خارج الدائرة

---

## 4) نوشن — الخريطة والحوكمة

**الجذر:** Business Partner 2.0 — «3 أبواب» (أشتغل/أعرض/أسلّم) و«6 أعمدة» (يومي · المبيعات · تطوير الأعمال · مراكز الأعمال · التقنية والأتمتة · المراجع والامتثال). البنية سليمة ومحوكمة بقواعد مكتوبة.

**كيف تعرف انعكاس نوشن على الموقع؟** المسار المعتمد:
1. **المحتوى**: صفحة «Business Partner Solutions» (المصدر المعتمد) ← تُنقل يدوياً/بجلسة كلود إلى `site/data/*.json` في GitHub ← البناء التلقائي ينشر
2. **الأسعار**: BP Services Catalog OFFICIAL (98 خدمة) ← `services.json` + قاعدة bp-quotes (136 خدمة) — **ثلاثة مصادر، بدأ ربطها بـ `/api/catalog/sync` (كوميت أمس) لكن لم يكتمل التفعيل**
3. **الخريطة**: صفحة «🔗 خريطة روابط الموقع» + «🗺️ الهيكلة والصفحات» (كل صفحة: محتواها + أتمتتها)
4. **التعديل السريع**: من `/admin` ← محرر المحتوى يكتب مباشرة في GitHub وينشر خلال دقائق — **هذه أسهل طريقة لك للتعديل**

**المصدر التنفيذي للوحة العميل:** «🧭 مركز عمليات العميل» — أي قرار جديد يوثق هناك أولاً.

---

## 5) ماذا بنيت فعلياً، وماذا بقي؟

**مبني ويعمل (مثبت):** موقع 1,098 صفحة بـ9 لغات · متجر خدمات كامل بالدفع · نظام توظيف ATS بفرز ذكي · 47 أتمتة نشطة · فريق 12 وكيلاً ذكياً · نظام عروض أسعار→عقد→فاتورة→سداد · لوحة تحكم موحدة بـCRM تفاعلي · إنبوكس واتساب مُدار · تحليلات زوار أولية الطرف · خلاصة يومية تلقائية · مساعد شخصي واتساب (مُعين).

**شبه مكتمل يحتاج تفعيلاً:** مزامنة الكتالوج الثلاثية · جسر الدفترة (مغلق) · DocuSign إنتاج · نفاذ (يعمل، تأكد من هويات المالك) · قالب واتساب التسويقي (بانتظار اعتماد ميتا).

**غير مبني/نموذج فقط:** Revenue OS الحقيقي (Login/عزل/بيانات فعلية) · تتبع تسويقي متقدم (UTM/تحويل) · النسخ الاحتياطي المنتظم · اختبارات آلية للمسارات الحرجة.

---

## 6) الزوار والضغطات — إجابة مباشرة

**نعم، أصبح عندك نظام قياس يملكه موقعك بالكامل** (منذ 24 أغسطس فقط):
- كل زيارة صفحة، وكل ضغطة على أزرار (أضف للسلة، إتمام الطلب، واتساب، أي زر data-track)، وكل خطأ جافاسكربت → تُسجل في Supabase وتظهر في `/admin` ← «إحصائيات الموقع»
- **الأرقام الفعلية (24–25 أغسطس):** 32 زائراً · 80 مشاهدة · 10 نقرات · 0 أخطاء. أعلى الصفحات: الرئيسية، /ar، /ar/suppliers، /ar/cart، /ar/partner-dashboard
- ⚠️ لا يوجد تاريخ قبل 24 أغسطس (النظام جديد)، وVercel Analytics غير مفعّل (يمكن تفعيله كطبقة ثانية مجانية مع Pro)

---

## 7) الصلاحيات — هل تملك كل شيء؟

**نعم — أنت المالك الوحيد الموثق على:** GitHub (businessKSA) · Vercel (Baher's projects) · Supabase (business partner org) · n8n · Make (2FA مفعّل ✓) · Notion workspace · Gmail/Workspace · حسابات الذكاء الاصطناعي.

**نقاط انتباه:** Apollo يحتاج إعادة توثيق · لا يوجد «مسؤول ثانٍ» احتياطي على أي منصة (خطر نقطة فشل واحدة — وثّق بيانات الاسترداد في مدير كلمات مرور) · مفاتيح اللوحة تفتح بـpanel key والأفضل حصرها بنفاذ (الخيار موجود بالكود: `اللوحة تفتح بنفاذ فقط 🔒`).

---

## 8) كم كلّفك المشروع؟

- **وقت:** ~3 أشهر مكثفة (يونيو→أغسطس): 265+ طلب دمج، 344 كوميت على فرع الإنتاج وحده، 98 وركفلو، مئات صفحات نوشن
- **اشتراكات جارية:** التقدير أعلاه ~SAR 400–900/شهر (الفواتير الدقيقة عندك في Gmail — ابحث عن invoices من n8n/Notion/Google/Anthropic/OpenAI لجمع الرقم الفعلي، أو كلّفني بجمعها في جلسة قادمة)
- **أكبر بند قادم إلزامي:** ترقيات P0 (~SAR 350/شهر إضافية)

---

## 9) خطة العمل المقترحة

**هذا الأسبوع (P0):** تدوير المفاتيح المكشوفة · RLS على bp-quotes · دمج فرع الإنتاج في master · repo→private · Vercel→Pro
**الأسبوعان القادمان (P1):** توسيع الكنس اليومي ليشمل Gmail غير المقروء + عروض bp-quotes + إحصائيات الزوار (تقرير يومي واحد عن كل شيء) · DocuSign إنتاج · اعتماد قالب واتساب · إعادة ربط Apollo
**الشهر القادم (P2):** تعبئة/أرشفة قواعد نوشن الفارغة · حذف وركفلوهات n8n المكررة · قرار Make وSlack · Revenue OS الحقيقي أو تجميده رسمياً

---

## 🟢 ملحق التنفيذ — نفس اليوم (2026-08-25)

| النتيجة الحرجة | الحالة | التفاصيل |
|----------------|--------|----------|
| RLS على bp-quotes | ✅ نُفذت | ترحيل `enable_rls_all_tables`: 23/23 جدولاً محمياً، صفر سياسات (anon مرفوض بالكامل). تحقق حي بعدها: `/api/catalog` يرد 200 بـ159 خدمة |
| المفاتيح المكشوفة في نوشن | 🟡 جزئياً | صفحة «BP Inbox — المفاتيح» نُقلت إلى Private، وقوائم تدوير وُضعت على رأس الصفحتين. التدوير الفعلي إجراء مالك |
| توحيد master مع الإنتاج | ✅ نُفذت | الكوميت `bfe3e699`: site/api/vercel.json من فرع الإنتاج، quotes/ من master حرفياً. الفرعان متطابقان، والنشران الإنتاجيان READY. متبقٍ للمالك: تغيير Production Branch لمشروع الموقع إلى master |
| المستودع عام | ⬜ إجراء مالك | GitHub → Settings → Danger Zone → Change visibility |
| Vercel Hobby | ⬜ قرار شراء | ترقية Pro ($20/شهر) تحتاج تأكيد إنفاق صريحاً |
