# سجل المشاريع — من يملك ماذا

أُنشئ 2026-09-24. **القاعدة الواحدة التي يقوم عليها هذا الملف:**

> كل صفحة ونظام لها **مشروع واحد** و**مالك واحد**. لا صفحة بلا مالك، ولا صفحة
> يملكها اثنان.

سبب وجوده: نُفِّذ العمل شهوراً بلا سجلٍّ لمن يملك ماذا، فبُني نظام جديد بجانب
القديم ولم يُغلق القديم — بوابتا عميل، ولوحتا مالك، وثلاث رحلات شراء. كل جلسة
تفتح الملف الذي تصادفه فتُصلحه، فتُضاعف العمل بدل أن تنهيه.

**قبل لمس أي صفحة: اقرأ من يملكها هنا.** إن لم تكن مذكورة، أضفها في الدفعة
نفسها — صفحة خارج هذا السجل هي الفوضى القادمة.

---

## المشاريع العشرة

| # | المشروع | المدير | يملك | الحالة |
|---|---|---|---|---|
| 1 | **البنية والمنصة** | Platform Engineer | البناء، النشر، التصميم الموحّد، Azure | ⚠️ التوحيد ناقص |
| 2 | **تجربة العميل** | Portal Owner | كل بوابة يدخلها عميل | 🔴 بوابتان لنفس الهدف |
| 3 | **عمليات المالك** | Ops Owner | كل لوحة يدخلها المالك | 🔴 لوحتان لنفس الهدف |
| 4 | **التوظيف** | Recruitment Manager | الوظائف وأصحاب العمل والمكاتب | ⚠️ ٢٢٤ صفحة قديمة |
| 5 | **الخدمات والمحتوى** | Content Curator | الكتالوج والخدمات والأدلة | ✅ موحّد |
| 6 | **الأتمتة والوكلاء** | Automation Engineer | n8n وواتساب والمستشارون | ✅ يعمل |
| 7 | **العروض والعقود** | Quotes & Contracts Manager | العرض والعقد والتوقيع و`quotes/` | 🔴 `bp-quotes` مشروع منفصل يشغّل عملاء |
| 8 | **محفول مكفول والرحلات** | Mahfol Manager | الخدمة ورحلات الشركات والمستثمر | ⚠️ لم يُجرَد |
| 9 | **السماسرة والإحالة** | Referral Manager | الوسطاء والعمولات وروابط الإحالة | 🔴 **غير مبنيّ** — شذرات متفرّقة فقط |
| 10 | **منتج B10X** | B10X Manager | المنتج وسعره وصفحته | 🔴 بلا SKU ولا سعر — لا يُشترى |

**قاعدة الإسناد التي تحسم التداخل:** أي صفحة **يسجّل فيها عميل دخوله** تتبع
«تجربة العميل» مهما كان محتواها. وأي صفحة **يدخلها المالك أو الفريق** تتبع
«عمليات المالك». هذا يمنع تنازع بوابة منتجٍ بين مشروع المنتج ومشروع البوابة.

---

## 1) البنية والمنصة — Platform Engineer

لا تملك صفحاتٍ يراها عميل؛ تملك ما يجعل كل الصفحات تُبنى وتُنشر وتبدو واحدة.

| المكوّن | الملف |
|---|---|
| سلسلة البناء (٢٢ خطوة) | `package.json` |
| حارس الـAPI (١٢/١٢ دالة) | `site/scripts/verify-api.mjs` |
| حارس الصفحات (١٤٨٤ صفحة) | `site/scripts/verify-pages.mjs` |
| بوابة النشر | `vercel.json` → `ignoreCommand` |
| طبقة التصميم الموحّد | `site/scripts/simple-v1-service-pages.mjs` |
| قواعد الجلسات | `CLAUDE.md` · `AGENTS.md` |
| Azure | `api/_azure.js` · `_azblob.js` · `_msgraph.js` · `_azpg.js` |

**مفتوح:** ٦١٥ صفحة خارج التصميم الجديد · ثلاث دفعات Azure متنافسة
(`#318`/`#319`/`#320`) · `api/_azpg.js` مكتوب وغير موصول.

---

## 2) تجربة العميل — Portal Owner

| الصفحة | التصميم | القرار |
|---|---|---|
| `/my` | ✅ جديد | **البوابة المعتمدة** (قرار المالك 2026-09-24) |
| `/account` | ❌ قديم | تبقى تعمل · لا تطوير فيها · **جُردت 2026-09-24** — تتقاعد بعد نقل ٣ أقسام |
| `/cart` | ✅ **جديد** | ✅ **أُنجز 2026-09-24** — القديمة على `/cart-classic` |
| `/checkout` | ✅ جديد | KEEP |
| `/quote` · `/contract` | — | KEEP |
| `/consultation` | — | KEEP |
| `/portal` (الموظف الذكي) | ❌ قديم | MOVE |
| `/partner-dashboard` | ❌ قديم | MOVE |
| `/compliance-dashboard` | — | MOVE |
| `/shared-services/dashboard` | ❌ قديم | MOVE |
| `/quotes/portal` | خارج الموقع | **MERGE** → `docs/quotes-cutover.md` |

> 🔴 **الازدواج الأول.** عميل واحد قد يملك ثلاثة حسابات: `/account` و`/my`
> و`/quotes/portal` — ولا يعرف أيّها حسابه. لا عمل على البوابتين قبل أن
> يُحسم أيّهما يبقى؛ وإلا فكل إصلاح مضاعف.

> 🔴 **انكسار مرئي.** `/cart` قديم و`/checkout` جديد: الهوية تتغيّر تحت يد
> العميل وهو يدفع. أقل إصلاح أثراً في الجهد، وأعلاه في الثقة.

---

## 3) عمليات المالك — Ops Owner

| اللوحة | التصميم | القرار |
|---|---|---|
| `/ops` | ✅ جديد | **اللوحة المعتمدة** (قرار المالك 2026-09-24) |
| `/admin` | ❌ قديم | تبقى تعمل · لا تطوير فيها · تُجرَد ثم تتقاعد |
| `/monitor` (BP Inbox) | ❌ قديم | MOVE |
| `/dashboard` (اختبار الوكلاء) | ❌ قديم | MOVE |
| `/suppliers-admin` | ❌ قديم | MOVE |
| `/agencies-admin` | ❌ قديم | MOVE |
| `/jobsearch-admin` | ❌ قديم | MOVE |
| `/doc-agent-admin` | ❌ قديم | MOVE |

> 🔴 **الازدواج الثاني.** `/admin` و`/ops` لوحتا مالك لنفس العمل. الدمج
> يوقف صيانة مزدوجة مستمرة.

---

## 4) التوظيف — Recruitment Manager

أكبر فجوة تصميم: **٢٢٤ صفحة** = ٣٦٪ من كل ما بقي قديماً.

| المجموعة | عدد | التصميم |
|---|---|---|
| `/jobs/*` | ١٥٢ | ❌ قديم |
| `/hr/employer/*` | ٧٢ | ❌ قديم |
| `/careers` · `/employers` · `/employer-join` · `/employer-login` | — | ❌ قديم |
| `/employer-dashboard` · `/agency-portal` | — | ❌ قديم |
| `/candidate-profile` · `/job` · `/job-search-service` | — | ❌ قديم |
| `/recruitment-agencies` · `/hiring` | — | مختلط |

**ملاحظة معلّقة (من `#323`):** جدولا `job_postings` و`job_applications` في
Supabase مكتوبان ومهاجَران، **ولا يقرأ منهما الموقع** — لوحة الوظائف تقرأ من
Notion. نظامان لنفس الشيء، يحتاجان قراراً.

---

## 5) الخدمات والمحتوى — Content Curator

| المجموعة | عدد | التصميم |
|---|---|---|
| `/services/<sku>` + التصنيفات | ~٢٠٠ | ✅ **جديد** |
| `/catalog` · `/packages` | — | ✅ جديد |
| صفحات المنتجات | ~١٥ | مختلط |
| `/knowledge-center` — مركز المعرفة | ٤ | ✅ **جديد** — الباب الوحيد إلى المعرفة: رابطٌ واحد في تذييل SV1 (2026-09-24) |
| `/calculators/government-cost` | ٤ | ✅ **جديد** (`simple-v1-gov-cost.mjs`، 2026-09-24) |
| كل صفحات مركز المعرفة: `/saudi-arabia` · `/directory` · `/opportunities` · `/news` · `/newsletter` · `/magazine` · `/tools-and-calculators` · `/calculators/{profession-checker,end-of-service,annual-leave,overtime,gosi}` | — | ✅ **جديد** — `sv1Page()` في generate.mjs: SV1.shell() + طبقة `SV1_LEGACY_CSS`، بلا main.js (النشرة والمجلة تحمّلان `assets/js/sv1-knowledge.js`) (2026-09-24) |
| `/calculator` · `/calculators/nitaqat` · `/magazine/print` | — | ❌ قديم — خارج مركز المعرفة (`/calculator` حلّ محلّه `/catalog` في القائمة) |
| `/guide/*` · `/saudi-arabia` · `/news` · `/magazine` | ٢٠+ | ❌ قديم — KEEP (محتوى) |
| `/mahfol-makfol` · `/trips` · `/tourism` | — | مختلط |
| `/about` · `/contact` · `/terms` · `/newsletter` | — | مختلط |

`/guide/company-structure` (الهيكلة والرواتب وتكلفة الموظف) — **أول دليل على
الموقع الجديد** (`SV1.shell()`، `site/scripts/simple-v1-guide-structure.mjs`).
نُشر أولاً على طبقة القديم ثم نُقل في اليوم نفسه تطبيقاً لـ§2.6. حلّ محلّ مسودة
`#81` (Astro المحذوف) التي أُغلقت دون دمج. نسخة Notion الموسّعة مرجع داخلي فقط
— **مصدر الحقيقة هو السكربت**. الأدلة الخمسة الأخرى ما زالت على القديم.

صفحات المنتجات: `/ai-agents` · `/ai-document-agent` · `/compliance-agent` ·
`/smart-employee` · `/shared-services` · `/business-development` ·
`/task-force` · `/data` · `/deals` · `/estrdad` · `/bank-account` ·
`/formation-contract` · `/b10x` · `/connect` · `/workspaces` ·
`/workspace-request` · `/worker-housing` · `/farina`.

**قواعد محتوى ملزمة** (تفصيلها في `CLAUDE.md` §4): الأسعار بالـSKU من
الكتالوج فقط · لا أسعار ولا معلومات حكومية مخترعة · المخالفات تُراجَع وتُقدَّم
ولا يُوعَد بإلغائها.

---

## 6) الأتمتة والوكلاء — Automation Engineer

| المكوّن | الحالة |
|---|---|
| n8n Cloud — ١٠١ سيناريو | ✅ يعمل |
| واتساب الإنتاج `BP-WhatsApp-Main` | ✅ **لا يُعدَّل مباشرة** |
| «باهر» — `api/chat.js` | ✅ Azure وحده |
| مشخّص الخدمة | ✅ على كل صفحة خدمة |
| `/team/*` — ١٢ مديراً | ❌ ٤٨ صفحة قديمة |
| `/chat` (Chat OS) | تجريبي |
| قائمة خدمات واتساب | ⚠️ تُولَّد آلياً، **تُلصق يدوياً** |

---

## صفحات بلا رابط إليها — مقصودة، فلا تُحذف

| الصفحة | روابط | الحكم |
|---|---|---|
| `/classic-home` | ٠ | **احتياطي مقصود** — الرئيسية القديمة |
| `/checkout-classic` | ٠ | احتياطي — يُؤرشف بعد استقرار الجديد |
| `/consultation-classic` | ٠ | احتياطي — نفس الحكم |
| `/simple-v1` | ٤ | رابط المعاينة قبل أن تصير الرئيسية — **مرشّح للحذف** |
| `site/_astro/` | — | ملفا CSS من موقع Astro المحذوف — **ARCHIVE** |

---

## قرارات المالك — 2026-09-24

| القرار | المعتمد | المؤجَّل |
|---|---|---|
| بوابة العميل | **`/my`** ✅ | `/account` |
| لوحة المالك | **`/ops`** ✅ | `/admin` |

السبب المُعلَن: «أبغى شيئاً سهلاً وبسيطاً». الجديدتان أبسط، وعليهما يُبنى.

### 🚫 قاعدة المالك (2026-09-24): التطوير في الجديد وحده

**ممنوع تطوير أي شيء على الموقع القديم إطلاقاً.** كل تحسين وكل ميزة وكل إصلاح
غير عاجل يذهب إلى الجديد: `/` و`/catalog` و`/cart` و`/checkout` و`/my`
و`/ops` — وما يُبنى عبر `SV1.shell()`.

القديم (`/account` · `/admin` · `/hr/employer/*` · صفحات `main.js`) يبقى
**يعمل ولا يُطوَّر**. ما يُسمح به فيه وحده: إصلاح عطلٍ يكسر عملاً تجارياً
قائماً — لا تحسين ولا ميزة ولا تجميل.

وأي ميزة في القديم يريدها المالك: **تُبنى في الجديد**، لا تُحسَّن في مكانها.

### ⚠️ شرط المالك: لا يُفقد عملٌ مبنيّ

**`/account` و`/admin` لا تُحذفان ولا تُعطَّلان.** فيهما شهورُ عمل وميزات قد لا
يكون لها مقابل في الجديدتين بعد. القاعدة:

1. **تبقيان مبنيّتين وتعملان** — روابطهما قد تكون في يد عميل أو في متصفح المالك.
2. **لا تطوير جديد فيهما** — كل ميزة جديدة تُبنى في `/my` أو `/ops`.
3. **قبل تقاعد أيٍّ منهما:** يُكتب جردٌ لما فيها وليس في الجديدة، ويُنقل ما
   يستحق. التقاعد بعد الجرد لا قبله.
4. **الجرد أُنجز 2026-09-24 — للبوابتين معاً.** الجرد الكامل (شاملاً `/admin`
   ومصدر `account.page.html` بأقسامه الواحد والعشرين) في
   **`docs/portals-inventory-2026-09-24.md`**، والملخّص التبويبي أدناه.

   **أهم ما كشفه، ولم يكن معروفاً:** القديمة والجديدة **قاعدتا بيانات
   مختلفتان** — `/admin` يقرأ نوشن و`/ops` يقرأ Supabase؛ و`/account` يمرّ على
   `api/requests` و`/my` على `api/simple`. الطلب الظاهر في إحداهما غير موجود
   في الأخرى، فالتقاعد **نقل بيانات لا نسخ واجهة**. وفي `/admin` أربع أدوات
   تمسّ المال والضريبة لا نظير لها في المستودع كله.

### جرد `/account` — ما فيها وليس في `/my` (2026-09-24)

استُخرج من `buildAccount()` في `site/scripts/generate.mjs` (سبعة تبويبات
`data-panel`) مقابل `buildSimpleMy()` في `site/scripts/simple-v1-my.mjs`
(عشرة أقسام).

| القسم في `/account` | له مقابل في `/my`؟ | القرار |
|---|---|---|
| مركز المهام (`overview`) | ✅ «الرئيسية» | لا نقل |
| طلباتي (`orders`) | ✅ «طلباتي» | لا نقل |
| بيانات المنشأة (`company`) | ✅ «بيانات الشركة» | لا نقل |
| **باقتي** (`package`) | ❌ **لا مقابل** | **يُنقل** |
| **مستندات المنشأة** (`documents`) | ❌ لا مقابل مستقل¹ | **يُنقل** |
| **محفظتي** (`wallet`) | ❌ **لا مقابل** | **يُنقل** |
| **مركز الدعم** (`support`) | ❌ لا مقابل مستقل² | **يُنقل** |

¹ `/my` فيها «مرفقات» داخل الطلب الواحد، لا خزانة مستندات للمنشأة كلها.
² `/my` فيها «المحادثات»، وقد تغني عن مركز الدعم — **يحتاج قرار المالك**.

**الاتجاه المعاكس** — ما في `/my` وليس في `/account`، وهو سبب اعتمادها:
عروض الأسعار · العقود · الفواتير · المواعيد · المحادثات · ابدأ طلب جديد.

**الخلاصة:** `/account` لا تتقاعد قبل نقل **ثلاثة أقسام مؤكدة** (باقتي ·
مستندات المنشأة · محفظتي) وحسم الرابع (مركز الدعم). بعدها تُبنى بلاحقة
`-classic` كما جرى مع `/cart-classic`.

> **قراءةٌ أعمق للمصدر نفسه** (`account.page.html`، ٣٨٠٣ أسطر — لا الصفحة
> المولَّدة) تضيف إلى الثلاثة أعلاه: **الضمان مع الموردين (Escrow)** ·
> **تعدّد الشركات** (حتى ١٠٠ منشأة بحساب واحد، بينما `/my` يعرف واحدة) ·
> **جسر مستندات bp-quotes** (عروض وعقود وفواتير بيد عملاء الآن، لا يراها
> `/my`). التفصيل في `docs/portals-inventory-2026-09-24.md`.

النمط المتبع في المستودع لحفظ صفحةٍ حلّت محلها أخرى: تُبنى بلاحقة `-classic`
ولا يشير إليها شيء في الموقع الجديد — كما جرى مع `/checkout-classic`
و`/consultation-classic` و`/cart-classic`. يُستعمل النمط نفسه عند التقاعد.

### ما زال يحتاج قراراً

- **وظائف: Supabase أم Notion؟** — جدولان مكتوبان لا يقرأ منهما الموقع.

---

## الأولويات

| # | المهمة | المشروع | أولوية |
|---|---|---|---|
| ~~١~~ | ~~`/cart` على التصميم الجديد~~ | تجربة العميل | ✅ **2026-09-24** |
| ~~٢~~ | ~~حسم `/account` ↔ `/my`~~ | تجربة العميل | ✅ `/my` معتمدة |
| ~~٣~~ | ~~حسم `/admin` ↔ `/ops`~~ | عمليات المالك | ✅ `/ops` معتمدة |
| ~~٤~~ | ~~جرد `/account` و`/admin`~~ | البوابات | ✅ **2026-09-24** |
| ٥ | **لوحة إحصائيات في `/ops`** — بكل المدد (انظر أدناه) | عمليات المالك | **P0** |
| ٦ | **وصل `attachment-add`** — الخادم يقبل المرفقات و`/my` لا يرسلها | تجربة العميل | **P0** |
| ٦ | حسم دفعات Azure الثلاث | البنية | **P0** |
| ٧ | مستشار المستندات يفشل بصمت في `fr` و`zh` | تجربة العميل | P1 |
| ٨ | ٢٢٤ صفحة توظيف | التوظيف | P1 |
| ٩ | توحيد الترويسة: نظاما ترويسة منفصلان (انظر أدناه) | البنية | P1 |
| ١٠ | ٤٨ صفحة فريق | الأتمتة | P2 |
| ١١ | أرشفة اللغات الناقصة (٢٥ صفحة) | الخدمات | P2 |

### 📈 الإحصائيات — الحالة والمطلوب (2026-09-24)

المالك بناها في `/admin` وهي مهمة عنده. الوضع الفعلي:

**ما يُجمَع** (أول طرف، بلا وسيط خارجي): مشاهدة لكل صفحة · نقرات الأزرار ·
أخطاء الجافاسكربت — تُرسل بـ`sendBeacon` إلى `/api/requests` بـ`action:"hit"`،
وتُخزَّن في `page_hits` و`site_errors`، ويقرؤها `?action=panel-analytics` عبر
أربع واجهات (`analytics_daily` · `_top_pages` · `_top_clicks` · `_top_refs`).

**العطل الذي كان قائماً وأُصلح اليوم:** العدّاد يعيش في `main.js`، وصفحات
Simple V1 لا تحمّله — فالرئيسية و`/catalog` و`/cart` و`/my` **لم تكن تُحسب
إطلاقاً**. أُضيف العدّاد إلى `SV1.shell()` بنفس عقد `main.js` حرفياً (نفس
`action:"hit"` ونفس مفتاح الزائر `bp_vid`) فتندمج السلسلتان بدل أن تنفصلا.
التغطية الآن ١٣٩٤ صفحة من ١٤٩٢؛ والـ٩٨ الباقية كلها صفحات قديمة لا تُطوَّر.

**ما ينقص** (مهمّ P0 في `/ops`):

| المطلوب | الحالة |
|---|---|
| زيارات يومية · أسبوعية · شهرية | ✅ موجودة في `/admin` |
| **ربع سنوية · نصف سنوية · سنوية** | ❌ غير موجودة — تُحسب من `page_hits` |
| الصفحات الأكثر زيارة · الأزرار · المصادر | ✅ موجودة (٧ أيام فقط) |
| حركة العميل داخل الموقع (رحلته) | ❌ البيانات موجودة (`visitor`) والعرض لا |
| البريد · واتساب · المستشار · لوحة العميل | ❌ لا تُقاس اليوم |

**دَينٌ يجب سداده:** `page_hits` و`site_errors` والواجهات الأربع **غير موجودة
في `db/schema.sql`** — أُنشئت يدوياً في Supabase. بيئة جديدة لن تحملها، والكود
يعتمد عليها. تُضاف إلى المخطط مع بناء لوحة `/ops`.

### لماذا ما زالت الصفحات تبدو مختلفة — السبب الجذري (2026-09-24)

ليس «تنسيقاً ناقصاً» بل **نظاما ترويسة منفصلان**:

- صفحات Simple V1 (`/` · `/catalog` · `/cart` · `/checkout` · `/my`) تُبنى عبر
  `SV1.shell()` بترويستها الخاصة، ولا تحوي `<header class="site-header">`.
- كل الباقي يمرّ على `simplified-global-header.mjs` الذي يستبدل تلك الترويسة
  بأخرى — قائمةٌ مختلفة وأزرارٌ مختلفة.

فالفرق في **محتوى القائمة** لا في الألوان وحدها، وتوحيده قرار محتوى لا تنسيق.

**محاولة سابقة فشلت صامتةً:** `simple-v1-service-pages.mjs` كان يحقن `class="sv1"`
في `<body>` الذي له صفة `class` أصلاً، فينتج `<body class="x" class="sv1">` —
والمتصفح يأخذ الأولى ويُسقط الثانية. ٨٦٩ صفحة حملت تنسيقاً **لم يُطبَّق قط**،
وHTML غير صالح. عُطّل السكربت من سلسلة البناء في 2026-09-24 (بقي الملف، ولم
يُحذف). و`.sv1` أصلاً يوضع على `div` داخلي لا على `body` — كما تفعل
`SV1.shell()`.

---

## خريطة ملكية الملفات — ما يمنع التصادم فعلياً

الجدول أعلاه يقول من يملك أي **صفحة**. هذا الجدول يقول من يملك أي **ملف** —
وهو الذي يمنع جلستين من الكتابة على شيء واحد. وكلاء Claude معرَّفون في
`.claude/agents/`، ولكل واحد نطاقه هنا.

| الوكيل | يملك هذه الملفات وحده |
|---|---|
| `platform-engineer` | `package.json` · `vercel.json` · `CLAUDE.md` · `AGENTS.md` · `docs/projects.md` · `site/scripts/verify-*.mjs` · `site/scripts/generate.mjs` · `site/scripts/simple-v1-service-pages.mjs` · `site/scripts/bump-b10x-cache.mjs` · `api/_azure.js` · `api/_azblob.js` · `api/_msgraph.js` · `api/_azpg.js` · `ops/azure/**` · `tests/azure-*.test.mjs` |
| `client-portal` | `site/scripts/simple-v1-my.mjs` · `simple-v1-checkout.mjs` · `client-portal-v6.mjs` · `sv1-quote-auth-flow.mjs` · `portal-trust-layer.mjs` · `api/_simple.js` · `api/pay.js` · `api/otp.js` |
| `owner-ops` | `site/scripts/simple-v1-ops.mjs` · `admin-command-center-v8.mjs` · `site/scripts/assets/admin.page.html` · `assets/monitor.page.html` · `api/requests.js` |
| `recruitment` | `api/candidate.js` · `candidates.js` · `employer.js` · `hire.js` · `api/_jobhunt.js` · `api/_agencies.js` · `site/scripts/*hiring*` · `site/scripts/*employer*` |
| `catalog-content` | `site/assets/data/catalog.json` · `site/data/*.json` · `api/_catalog.js` · `api/_knowledge.js` · `api/knowledge.json` · `site/scripts/simple-v1-guide-structure.mjs` · `site/scripts/simple-v1-gov-cost.mjs` · `site/scripts/simple-v1-knowledge.mjs` · `site/scripts/sv1-guide-sell.mjs` |
| `automation-agents` | `n8n/**` · `ops/n8n/**` · `api/chat.js` · `site/scripts/service-advisor.mjs` · `site/scripts/baher-support.mjs` · `site/scripts/assets/chat.page.html` |

### ملفات مشتركة — لا يملكها أحد وحده

| الملف | القاعدة |
|---|---|
| `db/schema.sql` | أي وكيل يضيف جدولاً، **ولا يعدّل جدول غيره**. الإضافة فقط. |
| `api/_db.js` | `platform-engineer` وحده — كل الوكلاء يستوردون منه |
| `site/**/*.html` | **مولَّدة، لا تُحرَّر يدوياً أبداً** — تُغيَّر بتغيير السكربت الذي يولّدها |
| `docs/platform-directory-*.md` | لقطة مؤرّخة — تُضاف إليها كتلة «حدث لاحقاً»، ولا يُعدَّل متنها |

### قواعد التصادم

1. **وكيل لا يكتب خارج نطاقه.** احتاج تغييراً في ملف غيره؟ يُرفع للمالك
   بجملة واحدة تقول: الملف، والسبب، والوكيل المالك.
2. **جلسة واحدة على الموقع في كل مرة.** حدث في 2026-09-23 أن دفعت جلستان في
   أربعين دقيقة على فرع الإنتاج نفسه.
3. **قبل كل دفعة:** `git fetch` ثم `git rebase` — جلسات أخرى تعمل الآن.
4. **الصفحات المولَّدة تتعارض دائماً.** تُحل بأخذ أي نسخة ثم `npm run build`.

---

## كيف يُحدَّث هذا الملف

يُحدَّث **في نفس الدفعة** التي تغيّر ملكية صفحة أو تضيف صفحة أو تحسم
ازدواجاً. سجلٌّ متأخّر عن الواقع أسوأ من غيابه: الغائب يدفعك للسؤال،
والمتأخّر يجعلك واثقاً وأنت مخطئ — وهو ما حدث مع رقم طلب الدمج في `CLAUDE.md`.


---

## الوكلاء الأربعة المضافون — 2026-09-24

طلب المالك أن يكون لكل موضوع وكيلٌ خاص لا يتداخل مع غيره، فأُضيف أربعة إلى
الستة. تعريفاتهم في `.claude/agents/`.

### قاعدة الاسم المزدوج — B10X

الاسم يسمّي شيئين لا علاقة بينهما، وخلطهما أخطأت فيه جلسات قبل اليوم:

| الاسم | ما هو | مالكه |
|---|---|---|
| **منتج B10X** | «نظام الهبوط في السوق السعودي» — خدمة تُباع | `b10x` |
| **طبقة B10X** | تصميم الموقع **القديم** على ١٤٩١ صفحة | `platform-engineer` |

`apply-b10x-theme.mjs` · `bump-b10x-cache.mjs` · `unified-b10x-experience.mjs`
· `b10x-services-catalog-v2.mjs` كلها **الطبقة القديمة**، وتزول مع النقل إلى
Simple V1. وكيل المنتج لا يلمسها.

### ما لا يملكه أحد بعد — وهذا مقصود

`broker-referral` نطاقه **لم يُبنَ**: لا ملف مخصّص له في المستودع، فقط شذرات
في `api/_simple.js` و`simple-v1-ops.mjs` و`db/schema.sql`. أولُ عمله تصميمٌ
يُعرض على المالك، لا كود. وأربعة أسئلة مالٍ تسبق أي سطر: نسبة العمولة، ومتى
تُستحقّ، ومن أين تُخصم، وماذا يجري عند الاسترجاع بعد صرفها.

### حدود المحادثة الواحدة — ما تستطيعه الجلسة الرئيسية وما لا تستطيعه

المالك يدير اليوم ستّ عشرة محادثة منفصلة في claude.ai، كلٌّ على موضوع.
**الجلسة الرئيسية لا تقرأ تلك المحادثات ولا ترسل إليها أوامر** — كل محادثة
معزولة عن الأخرى، وهذه حقيقة المنتج لا نقصٌ في الإعداد.

ما يمنع التداخل فعلاً ليس الأوامر بل **الملفات**: كل جلسة تفتح هذا المستودع
تقرأ `CLAUDE.md`، وفيه جدول الملكية. فالتنسيق يجري عبر المستودع لا عبر
المحادثات. ومن أراد توزيعاً حياً من مكان واحد، فمكانه هذه الجلسة عبر أداة
`Agent` — لأن الوكيل الفرعي لا يشغّل وكيلاً آخر.
