# أزور — الحالة وخطة الهجرة

قرار المالك (2026-09-16): **«اربط كل الأوتوميشن والشاتنج والمعلومات مع أزور
مايكروسوفت، وما تستخدم أي شيء غير أزور.»**

الرصيد: Microsoft for Startups — **100,000$** تنتهي **8 سبتمبر 2028**.
الاشتراك: `b354e2b9-ecb6-4407-9063-18fbcb87986a`.

> هذا الملف يُحدَّث بحالة **متحقَّق منها** فقط. كل سطر «تم» تحته فحص حيّ
> يمكن لأي جلسة إعادته. لا يُكتب «تم» على كود مكتوب لم يُفحص بعد النشر.

## الموارد القائمة

| المورد | النوع | المنطقة |
|---|---|---|
| `bp-ai-ksa-2026` | Azure OpenAI | Sweden Central |
| `drbahermagnas-6763-resource` | AI Foundry | East US 2 |

النشرتان على `bp-ai-ksa-2026`:

| النشرة | النموذج | السقف | لِمَن |
|---|---|---|---|
| `bp-main` | GPT-4.1 | 1M TPM | الردود والتحليل وقراءة المستندات |
| `bp-fast` | GPT-4.1-mini | 5M TPM | المسارات عالية الحجم |

**اسم النشرة هو ما يُكتب في حقل النموذج**، لا اسم الموديل. هذا أشيع خطأ هنا.

## الحالة — 2026-09-24

| النظام | على أزور؟ | الدليل |
|---|---|---|
| محادثة الموقع (`api/chat.js`) | ✅ **وحده** | `GET /api/chat` → `providers: ["azure","baher-n8n"]` |
| الصوت والتفريغ | ✅ | نفس الفحص، حقل `voice` |
| التوظيف (`api/hire.js`) | ✅ | `api/_azure.js` أول السلسلة |
| قراءة المستندات (`api/_docread.js`) | ✅ | نفس الوحدة |
| بريد باهر | ✅ | Azure Communication Services |
| التخزين | ⚠️ جزئي | `api/_azblob.js` موجود · Drive ونوشن ما زالا يحملان ملفات |
| نماذج n8n | ✅ | لا نموذج غير أزور في أي مسار حيّ — التفصيل تحت |
| قاعدة البيانات | ❌ | Supabase + Notion + جداول n8n |
| الاستضافة | ❌ | Vercel + n8n Cloud |

**المزوّدات الأربعة السابقة (Gemini · Groq · Anthropic · OpenAI) حُذفت من
`api/`.** لا تُعِدها. الاحتياطي الوحيد الباقي هو `baher-n8n` وهو بلا مفتاح،
لكنه يمرّ عبر n8n. كان ذلك يعني أن سقوط أزور يقع على نماذج غير أزور — ولم يعد
كذلك: ورك فلوهات n8n صارت هي نفسها على أزور، فالاحتياطي يعود إليه.

## المرحلة ١ — النماذج: مكتملة (2026-09-24)

كان هذا الملف يقول إن **اعتماد `Azure OpenAI` في n8n غير موجود** وإنه المعطِّل
الوحيد. الاعتماد موجود ويعمل: `Azure Open AI account` نوعه `azureOpenAiApi`،
المعرّف `hA1ULr5qtFuoBZ6B`. **لا تطلب من المالك إنشاءه مرة أخرى.**

### دليل قويّ — تنفيذ حيّ

| الورك فلو | ID | الدليل |
|---|---|---|
| 🛡️ مشاري (الامتثال) | `milR3qpw3c9Li5xN` | تنفيذ `112719` نجح على `Azure OpenAI (bp-main)` بـ`engine_fallback:false` |
| 🧠 مُعين | `YgRwn40v1CqsscJw` | تنفيذ `108333` — نفس العقدة، ومعها `Conversation Memory` |

### دليل أضعف — توصيل العقد لا تنفيذ

هذان لم يُفحصا بتنفيذ حيّ: `BP-Sub-AI-Conversation` بلا أي تنفيذ مسجّل،
وتنفيذات الأوركستريتر الناجحة تقف عند `IF - Media Type` و`metadata` فارغ.
فالفحص على التوصيل، والتمييز مقصود.

| الورك فلو | ID | ما وُجد |
|---|---|---|
| BP-Chat-Suggest | `B0pF8xB7b4L3hxtd` | العقدة الموصولة `Azure OpenAI (bp-main)` |
| BP-Sub-AI-Conversation | `ETxqAM5VooDiDJDN` | `lmChatAzureOpenAi` موصولة بـ`AI Agent`، نشرة `bp-main` |
| BP-Sub-Documents | `Sn2vTAQzU1PuWuDs` | موصول بـ`Azure OpenAI (bp-main)` |
| BP-WhatsApp-Main | `tIb4wNOSYVTZQuox` | **لا نموذج فيه أصلاً** — ٧٨ عقدة بلا عقدة langchain؛ يفوّض عبر `executeWorkflow` |
| BP-Sub-Advisor | — | بلا ذكاء اصطناعي |

بقية وكلاء الفريق يشتركون مع مشاري في القالب نفسه، فالأرجح أنهم عليه —
**لكنهم لم يُختبروا فرداً فرداً، فلا يُكتب لهم «تم».**

## دَين تنظيف — عقد مزوّدين مفصولة

باقية في الورك فلوهات، **غير موصولة وغير معطّلة**. لا تُنفَّذ، لكن وصلها سهواً
يعيد مزوّداً حُذف عمداً:

| الورك فلو | العقدة اليتيمة |
|---|---|
| BP-Chat-Suggest | `Claude Draft Reply` · `OpenAI Draft Reply` |
| BP-Sub-AI-Conversation | `OpenAI Chat Model` (`gpt-4o-mini`) |
| BP-Sub-Documents | `OpenAI Chat Model5` (`gpt-4o-mini`) · `Groq Engine` |

ويُصحَّح كذلك **وصف `BP-WhatsApp-Main`**: يقول إنه على `gpt-4o-mini` وهو لا
يحمل نموذجاً إطلاقاً. الوصف الخاطئ أخطر من غيابه — منه جاء ظنّ أن الواتساب
خارج أزور.

## المراحل التالية

**المرحلة ٢ — البيانات.** نوشن → Cosmos DB · المستندات → Blob Storage ·
قاعدة المعرفة → AI Search. السبب: مصدر الحقيقة اليوم موزّع على خمسة أماكن
(نوشن · جداول n8n · Supabase · Drive · Blob)، وهذا أكبر دين تقني في النظام.

**المرحلة ٣ — الاستضافة.** n8n Cloud ليس أزور؛ البديل Container Apps +
PostgreSQL Flexible Server. ينقل كل الويبهوكات، فيحتاج نافذة صيانة معلنة.
الموقع: Vercel → Static Web Apps، وهذا يلغي `vercel.json` بكل قواعده
(ومنها `ignoreCommand` الذي يحمي سقف النشرات). **لا تبدأ قبل استقرار ٢.**

## قاعدة الهجرة: لا توقّف ولا فقد بيانات

ابنِ الجديد على أزور والقديم يعمل → انسخ البيانات → اربط → اختبر →
شغّل الاثنين معاً → حوّل جزءاً صغيراً من الاستخدام → راقب → زِد تدريجياً →
حوّل بالكامل → **أبقِ خطة رجوع** → أغلق القديم آخر شيء وبعد تأكد.

## قواعد لا تُكسر

- **المفتاح لا يُكتب في مستودع ولا في نوشن ولا في محادثة.** مكانه متغيّرات
  بيئة Vercel واعتمادات n8n. وعند بناء Key Vault يصير هو المصدر.
- n8n Cloud يمنع `$env` في التعبيرات — أي مفتاح هناك يكون في اعتماد لا تعبير.
- لا تُعِد أي مزوّد غير أزور إلى `api/`.
- الدليل على «تم» فحص حيّ بعد النشر، لا كود مكتوب — وإن تعذّر الفحص الحيّ
  فاكتب أن الدليل على التوصيل، لا تُسكت الفرق.

## ما ينقص النظام: مراقبة الرصيد

نفد رصيد مزوّد الذكاء مرتين في شهرين (Anthropic في يوليو، OpenAI وGemini في
أغسطس)، واكتُشف في المرتين من **شكوى عميل لا من تنبيه**. أزور اليوم مزوّد
وحيد بلا احتياطي خارجي، فنفاد رصيده يوقف كل شيء دفعة واحدة. مراقبة يومية
تنبّه قبل النفاد هي أهم ما تبقّى بعد المرحلة ١.

## ملاحظة للجلسات القادمة

جلسات Claude Code في الحاوية السحابية **لا تصل إلى أزور**: `az` غير مثبّت،
و`management.azure.com` محجوب بسياسة شبكة البيئة، ولا توجد متغيّرات أزور
في البيئة. أي عمل يحتاج إنشاء موارد أزور يُنفَّذ إما من جهاز المالك بعد
`az login`، أو بعد توسيع سياسة الشبكة وإضافة اعتماد للبيئة.

## تحذير تشغيلي: الفرع المشترك يُعاد كتابته

هذا الملف سقط مرة واحدة فعلاً: أُضيف في `3ae89cb5` ثم اختفى حين أُعيدت كتابة
`claude/bpic-marketing-site-jvrnga` قسرياً في 2026-09-24، ولم يكن قد وصل
`master` بعد. أي جلسة تضيف معرفة لا تُشتق من الكود (أرقام اشتراكات، أسماء
موارد، قرارات مالك) تضعها في مسار يصل `master` سريعاً، ولا تتركها على فرع
مشترك تتزاحم عليه عدة جلسات.
