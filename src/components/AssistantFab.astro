---
// زر عائم يفتح المساعد الصوتي الذكي (الفريق التنفيذي) — يظهر في كل الصفحات
---

<a
  href="/voice"
  class="assistant-fab"
  aria-label="المساعد الذكي — تحدّث بالصوت"
>
  <span class="af-icon" aria-hidden="true">🎙️</span>
  <span class="af-label">المساعد الذكي</span>
</a>

<style>
  .assistant-fab {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    z-index: 99;
    display: flex;
    align-items: center;
    gap: 0.55rem;
    height: 60px;
    padding: 0 20px;
    border-radius: 999px;
    background: linear-gradient(135deg, #12b3ad, #0e7f7b);
    color: #fff;
    text-decoration: none;
    box-shadow: 0 10px 30px rgba(14, 159, 154, 0.4);
    font-family: inherit;
    font-weight: 700;
    font-size: 0.95rem;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .assistant-fab:hover {
    transform: translateY(-2px) scale(1.03);
    box-shadow: 0 14px 36px rgba(14, 159, 154, 0.5);
  }
  .af-icon {
    font-size: 1.5rem;
    line-height: 1;
  }
  .af-label {
    white-space: nowrap;
  }
  @media (max-width: 768px) {
    .assistant-fab {
      padding: 0;
      width: 60px;
      justify-content: center;
      gap: 0;
    }
    .af-label {
      display: none;
    }
  }
</style>
