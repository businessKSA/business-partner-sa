# دستور العمل في هذا المستودع — يُقرأ في بداية كل جلسة Claude Code

> المالك: باهر. هذا الملف هو التعميد الموحّد لكل الجلسات. أي جلسة تخالفه تُعتبر خارج الصلاحية.
> آخر تحديث: 2026-09-02.

## 1) النشر على Vercel مجمّد من الجلسات — نهائياً

- **لا نشر على Vercel من أي جلسة** بأي طريقة: لا `deploy_to_vercel`، لا Deploy Hook، لا `vercel deploy`، لا كوميت فارغ لإطلاق بناء، لا إعادة نشر (Redeploy).
- **لا دفع مباشر إلى `master` ولا إلى فرع الإنتاج `claude/bpic-marketing-site-jvrnga`** ولا إلى `staging`. هذه الفروع الثلاثة يدفع إليها المالك وحده (بالدمج من GitHub).
- كل جلسة تعمل على فرعها الخاص `claude/<اسم>` وتفتح PR كمسودة (draft) إلى `master`. **الدمج قرار المالك فقط** بكلمة صريحة: «ادمجه». لا يدمج أي PR تلقائياً ولا يوافق على أي PR.
- `vercel.json` يعطّل النشر لكل الفروع ما عدا `master` و`staging` (`git.deploymentEnabled`). لا تعدّل هذا القسم ولا تلتف عليه.
- لا تنشئ تذكيرات (`send_later`) أو حلقات فحص تعيد الدفع لإعادة تشغيل فحوص Vercel. فحص Vercel الأحمر بسبب `api-deployments-free-per-day` أو تعطيل الفرع **ليس خطأً في الكود** ولا يُعالَج بدفعة جديدة.
- التصحيح الحي على الإنتاج (hotfix عشوائي) ممنوع. أي إصلاح يمر بنفس المسار: فرع ← PR ← staging ← اختبار المالك ← master.

## 2) مسار الاعتماد: كيف يصل التعديل إلى الموقع

1. الجلسة تنجز العمل على فرعها وتتحقق محلياً (القسم 3) ثم تدفع وتفتح PR مسودة.
2. المالك يراجع الـPR ويدمجه في `staging` (فرع المعاينة الوحيد؛ رابطه الثابت في مشروع Vercel: `business-partner-sa-businessksa-git-staging-*.vercel.app`).
3. المالك يجرّب كل الخصائص على رابط staging بقائمة الفحص في القسم 4.
4. إذا اكتمل الفحص بلا أخطاء يدمج المالك `staging` في `master`، وmaster هو فرع الإنتاج (Production Branch في Vercel يجب أن يكون `master`).
5. نشرة واحدة للمعاينة ونشرة واحدة للإنتاج لكل دفعة اعتماد. لا شيء غير ذلك.

## 3) التحقق المحلي الإلزامي قبل أي دفع

من جذر المستودع:

```bash
npm run build      # يولّد الموقع الساكن (site/) — يجب أن ينجح
npm test           # اختبارات الجذر — يجب أن تنجح كلها
node --input-type=module -e "import('./api/requests.js')"   # الـAPI يُحمّل بلا خطأ
```

لتطبيق عروض الأسعار (`quotes/`): `npx prisma generate && npx tsc --noEmit && npm test && npx next build`.
لا تدفع إن فشل أي أمر. ولا تهمل الفشل باعتباره «عابراً».
ولا تعدّل ملفات `site/**/*.html` المولَّدة يدوياً؛ عدّل المصدر ثم `npm run build`.

## 4) قائمة فحص المالك على staging (تُرفق في وصف كل PR كبنود يمكن تعليمها)

- الصفحة الرئيسية بالعربية والإنجليزية وقائمة التنقل والفوتر.
- السلة → الدفع → صفحة الحساب (بطاقة اختبار ميسر، وتحويل بنكي).
- نموذج التواصل والاستشارة والتسجيل (يصل إلى نوشن/البريد).
- بوابة العميل: عرض سعر ← توقيع ← فاتورة ← سداد.
- لوحة `/admin` و`/monitor` تفتحان بالمفتاح وتقرآن البيانات.
- التوظيف: تقديم مرشح، دخول صاحب عمل، بحث في المخزن.
- الوكلاء الأذكياء يردون (الموقع والواتساب).
- لا أخطاء في كونسول المتصفح ولا 500 في سجلات Vercel للمعاينة.

## 5) التوثيق في نوشن — إلزامي في نهاية كل جلسة وعند كل توقف

قاعدة **«🧭 سجل جلسات Claude Code»** في نوشن (تحت: Business Partner 2.0 ← مركز الموقع الإلكتروني ← التطوير والنشر والمنصّات).
لكل جلسة صفحة واحدة تُحدَّث لا تُكرَّر، بالخصائص: ماذا طُلب، ماذا أُنجز، أين وقفت، أخطاء أُصلحت، أخطاء ما زالت، يحتاج منك، الفرع وطلبات الدمج، الأدوات، رابط الجلسة `https://claude.ai/code/<session_id>`.
ابحث عن صفحة جلستك بمعرّفها أولاً (`notion-search` في القاعدة)، وحدّثها بـ`notion-update-page`؛ إن لم توجد أنشئها. لا تنشئ قواعد جديدة.

## 6) قواعد عامة

- الأسرار لا تُكتب في الكود ولا في نوشن ولا في وصف PR. تُضبط في Vercel → Environment Variables بيد المالك.
- الأرقام: واتساب الشركة 0530540231 للتواصل البشري، 0507034157 للوكيل الآلي فقط. الرقم الشخصي لا يظهر لأي عميل.
- اللغة مع المالك: العربية. رسائل الكوميت بالعربية أو الإنجليزية، واضحة ومحددة.
- حد Vercel للدوال 12 دالة: أي مسار API جديد يُضاف كـ`__route` داخل `api/requests.js` لا كملف جديد.
- عند الشك في قرار يؤثر على الإنتاج أو الأسعار أو العملاء: توقف واسأل المالك، لا تفترض.
